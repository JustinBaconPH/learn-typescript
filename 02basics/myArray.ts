const students: string[] = []
// const studentClass: number[] = []
const studentClass: Array<number> = []

type User = {
    name: string
    isActive: boolean
}

const allUsers: User[] = []

const MLModels: number[][] = [
    [255,255,255],
    []
]

students.push("MisonoMika")
studentClass.push(2)

allUsers.push({name: "", isActive: true})